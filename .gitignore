# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/
# 敏感配置（含 Supabase 凭据，绝不推送）
.env
.env.local
.env.*.local

# 运行时产物
dev.log
server.log
mini-services/chat-service/service.log

# 数据库文件（本地 SQLite，已弃用但保留）
db/*.db
db/*.db-journal

# 上传文件（运行时产生）
uploads/*
!uploads/.gitkeep

# 打包产物
study-island.tar.gz
study-island.zip

# agent 工作记录（私密）
agent-ctx/
.zscripts/
tool-results/
